# ESP-TAMP Demo

Demonstrates data compression and decompression using the Tamp library on ESP32 chips.

The file "espressif-logo.bmp" is embedded into the application and used as input data for
compression. The compressed data is then decompressed again and verified to be identical
to the input.
